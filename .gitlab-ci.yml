image: ubuntu

stages:
    - test

deploy-dev:
    stage: test
    only:
        - web
    script:
        - apt install pip
        - pip install --user -r requirements.txt 
        - pip install --user pytest
        - pytest
